name: PR to seed-nodejs

on:
  workflow_dispatch:
  push:
    branches: 
      - main
     
jobs:
  create-PR:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
        with:
          repository: SchemaStore/schemastore

      - name: Sync local fork 
        run: | 
          git remote add localfork git@github.com:sapiderman/schemastore.git  
          git push localfork master    
      
      - name: Update file
        run: |          
          rm README.md
          wget https://raw.githubusercontent.com/hyperjumptech/monika/main/README.md          
          cat README.md   
          git status
          ls -s    
      

      - name: Check outputs
        if: ${{ steps.cpr.outputs.pull-request-number }}
        run: |
          echo "Pull Request Number - ${{ steps.cpr.outputs.pull-request-number }}"
          echo "Pull Request URL - ${{ steps.cpr.outputs.pull-request-url }}"



